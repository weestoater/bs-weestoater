(function () {
  // set up the utils class
  var utils = {
    /**
     * Runs a test
     * @function
     */
    test: function () {
      // Test function - logs removed for production
    },
  };

  // execute the test()
  utils.test();
})();
