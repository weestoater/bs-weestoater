(function () {
  // set up the utils class
  var utils = {
    /**
     * Runs a test
     * @function
     */
    test: function () {
      /**
       * Logs a message to the console
       */
      console.log("This is a test for the IIFE");
    },
  };

  // execute the test()
  utils.test();
})();
